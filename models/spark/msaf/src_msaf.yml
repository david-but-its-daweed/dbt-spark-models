version: 2

models:
  - name: table_with_comments
    description: Testing persisting table documetation
    columns:
      - name: field1
        description: (from airflow run) first field. Its a number
      - name: field2
        description: (from airflow run) second field. Its a string
