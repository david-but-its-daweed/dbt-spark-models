version: 2

seeds:
  - name: slo_tables_seed
    config:
      schema: platform

  - name: slo_details_seed
    config:
      schema: platform
  
  - name: product_outliers_seed
    config:
      schema: mart